name: pr-check

on:
  pull_request:
    branches: ['*']

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/test.yaml
    with:
      aws-environment: dev
      # ref intentionally omitted: defaults to '' so checkout uses the PR head
